function projectsCreation(input){
    let architect = input[0];
    let project = input[1];
    console.log(`The architect ${architect} will need ${3 * project} hours to complete ${project} project/s.`);

}